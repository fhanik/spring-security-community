server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    sample: INFO

spring:
  thymeleaf:
    cache: false
  security:
